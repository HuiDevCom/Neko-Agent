name: Neko-Agent
version: '${version}'
main: org.agent.agent.Agent
api-version: '1.21'
load: POSTWORLD
icon: icon.png

commands:
  agent:
    description: 与 Neko-Agent 对话
    usage: /agent <消息>
    aliases: [nekoagent, na, neko, agent]

permissions:
  agent.admin:
    description: 允许重载配置和清除所有对话历史
    default: op
